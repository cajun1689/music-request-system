{
  "name": "dj-bridge",
  "version": "1.7.1",
  "description": "Menubar Mac app that reads Rekordbox or Serato now-playing tracks and pushes them to the Casper Requests cloud API",
  "author": "Casper Requests <admin@casperrequests.com>",
  "repository": "https://github.com/cajun1689/music-request-system",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "tsc && electron .",
    "build": "tsc && rm -rf dist/renderer && cp -r src/renderer dist/renderer && cp src/main/preload.js dist/main/preload.js && node scripts/stamp-version.js",
    "package": "tsc && electron-builder --mac",
    "postinstall": "electron-builder install-app-deps"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "electron-log": "^5.2.0",
    "electron-store": "^8.2.0",
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "@electron/notarize": "^2.5.0",
    "@types/better-sqlite3": "^7.6.11",
    "electron": "^31.0.0",
    "electron-builder": "^25.0.0",
    "electron-rebuild": "^3.2.9",
    "typescript": "^5.5.0"
  },
  "build": {
    "appId": "com.casperrequests.dj-bridge",
    "productName": "DJ Bridge",
    "publish": {
      "provider": "github",
      "owner": "cajun1689",
      "repo": "music-request-system"
    },
    "mac": {
      "category": "public.app-category.music",
      "icon": "assets/icon.icns",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "entitlements.mac.plist",
      "entitlementsInherit": "entitlements.mac.inherit.plist",
      "notarize": true,
      "target": [
        {
          "target": "dmg",
          "arch": ["universal"]
        },
        {
          "target": "zip",
          "arch": ["universal"]
        }
      ]
    },
    "dmg": {
      "title": "DJ Bridge ${version}",
      "artifactName": "DJ-Bridge-${version}-mac.${ext}"
    },
    "directories": {
      "output": "out"
    },
    "files": [
      "dist/**/*",
      "assets/trayTemplate.png",
      "assets/trayTemplate@2x.png",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "assets/trayTemplate.png",
        "to": "trayTemplate.png"
      },
      {
        "from": "assets/trayTemplate@2x.png",
        "to": "trayTemplate@2x.png"
      }
    ]
  }
}
